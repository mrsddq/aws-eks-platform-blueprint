apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Explicit resources avoid ingesting Helm values and the Argo Application itself.
resources:
  - namespaces.yaml
  - rbac.yaml
  - app/deployment.yaml
  - app/service.yaml
  - app/ingress.yaml
  - app/autoscaling.yaml
